# Проверка — 8 сентября 2026

Статус: **проверка в Unity ещё не выполнена**. Исходная папка была пустой, пользовательских изменений не перезаписывали. Unity Hub обнаружен; `unity editors --installed --json` вернул пустой список. Загрузка через CLI оставалась на нуле. Прямой официальный адрес доступен; загрузка продолжена через curl. Скрипт `Tools/Setup-And-Build.ps1` докачивает, проверяет установщик, устанавливает Editor и пробует собрать Windows. Фактическое текущее состояние: `TestResults/setup-status.json`.

## Выполнено

- `powershell -ExecutionPolicy Bypass -File .\Tools\Test-Core.ps1`: **12 passed, 0 failed**. Компилятор .NET Framework 4, тестируется реальный `ShipSimulation.cs`, без подмены математической модели.
- Сценарии: стоянка на якоре, границы паруса, разгон, торможение, инерция после уборки паруса, поворот, возврат руля, повторяемость команд, предел карты, сброс, остановка у препятствия, некорректные команды, границы высоты волн.
- Исходный отчёт: `TestResults/core-tests.txt` (генерируемый файл, не включён в Git).
- Статически проверены разделение runtime/editor-кода, использование Input System, формулы волн C#/HLSL, границы движения на палубе и удержание ссылок на шейдеры для сборки.
- Настройка URP использует фабрику `UniversalRenderPipelineAsset.Create(renderer)` из исходников Unity 6.0.

## Ещё не подтверждено

| Критерий | Состояние |
|---|---|
| Импорт пакетов и компиляция всех Unity-скриптов | Ожидает Editor |
| Компиляция шейдера | Ожидает Editor |
| Создание и открытие FirstVoyage | Ожидает Editor |
| Управление мышью и клавиатурой в Play Mode | Не запускалось |
| Визуальное качество корпуса, моря и HUD | Не проверялось |
| Сборка Windows и запуск EXE | Не выполнялись |
| Производительность на слабом ПК | Не измерялась |
| Сетевой кооп | Не реализован в этапе 1 |

Полный план ручной проверки: `Docs/Playtest.md`. Прохождение C#-тестов не доказывает готовность Unity-сцены или Windows-сборки.
